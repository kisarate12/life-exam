# 人生全国共通模試 Ver1 全体方針（段階改修）

改修時はこの方針に従い、既存の動く実装をベースに段階改修する。全面作り直しは最後の手段。

---

## 1. プロダクトの狙い

- **人生全国共通模試** = 模試形式（志望タイプ → 受験 → 合否）の"エンタメ模試"。心理診断ではない。
- 体験価値：**テンポの良さ** と **模試っぽい結果演出**（偏差値・判定・レーダー）。

---

## 2. 固定の骨格

### 2-1. 5教科（スコア領域）

| DB code       | Ver1表示 |
|---------------|----------|
| financial     | 経済（お金） |
| time          | 自由（時間） |
| social        | 関係（人間関係：友人＋家族＋恋人） |
| psychological | 心理（メンタル安定） |
| human         | 意味（人生の方向性） |

### 2-2. 志望タイプ（A〜E＝志望校の役割）

- **A 安定型**：生活の安定・再現性（経済・心理やや重め）
- **B 高収入型**：収入最大化（経済最重）
- **C 自由型**：時間の自由（自由を最重）
- **D 人間関係重視型**：友人/家族/恋人の充実（関係を最重）
- **E 自己実現型**：方向性/使命/成長（意味を最重）

---

## 3. Ver1 改修順（厳守）

1. **志望タイプ追加**（済）
2. **50問化**（5教科×10問＝50問固定）
3. **志望タイプ倍率**（教科ごと比重で総合点算出）
4. **同年齢偏差値**（norm 参照で「同年齢平均との差」を必ず表示）
5. **version 管理**（将来の質問差し替え前提で exam_version 等を先に入れる）

---

## 4. 体験フロー（Ver1 目標）

1. トップ
2. **志望タイプ選択（A〜E）**
3. 基本情報（年齢・性別・居住地のみ）
4. 50問回答（マーク式／テンポ重視）
5. 結果：総合偏差値、判定（S/A/B/C/D 等）、5教科レーダー、**同年齢平均との差（必須）**、（可能なら）全国平均との差

---

## 5. 同年齢偏差値（norm 方式）

- 個票の大量生成ではなく、**年齢別の基準値（norm）参照**で偏差値を算出。
- norm は「年齢×志望タイプ×教科（＋総合）」の mean/std を持つ前提。
- 実データが溜まったら、norm を実測ベースへ置換できる設計が望ましい。

---

## 6. 設問設計（Ver1）

- 基本は **4段階（A〜D）**。事実レンジ設問のみ **5段階（A〜E）**。
- 50問は Ver1 で固定。将来変更する前提で **version 管理**。

---

## 7. やらないこと（MVP 線引き）

- 分析軸をユーザーが選ぶ UI（Ver2 以降）
- 属性項目の追加（年収・資産・職業など）※既存項目は維持
- 高度なランキング、共有、AI コメント生成
- 途中保存や演出の大幅強化（まずは完走優先）

---

## 8. コード上の参照

- 志望タイプ・5教科の表示名・説明：`apps/web/lib/life-exam/ver1-concepts.ts`
- 志望タイプ選択肢：`profileOptions.ts` の `ASPIRATION_TYPE_OPTIONS`
- スコア計算：`lib/life-exam/scoring.ts`（50問化で `QUESTIONS_PER_SUBJECT=10` に変更予定）
- 倍率：未実装。志望タイプ別の教科ウェイトを定数化し、`computeTotalAndDeviation` 等で参照する想定。
